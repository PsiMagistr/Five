/**
 * Created by Pavel on 05.04.2021.
 */

class Point
{
    constructor(x, y){
       this.x = x;
       this.y = y;
    }
}


class Ceil
{
    constructor()
    {
        this.ball = 0;
        this.fieldColor = "";
        //scena.fillRect(x * size + 1 * (x + 10), y * size + 1 * (y + 10), size, size);

    }
}
